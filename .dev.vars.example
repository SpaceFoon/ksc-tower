# Cloudflare Workers local vars (wrangler). Same names as Astro PUBLIC_ build env.
# Copy to `.dev.vars` for `wrangler dev` / `astro preview` against the Worker.

PUBLIC_GA_MEASUREMENT_ID=
PUBLIC_POSTHOG_PROJECT_TOKEN=
PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
